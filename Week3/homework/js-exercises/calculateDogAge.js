'use strict';
function calculateDogAge(age) {
  let dogAge = age * 7;
  return console.log(`Your doggie is ${dogAge} years old in dog years!`);
}
calculateDogAge(1);
calculateDogAge(3);
calculateDogAge(8);
